import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
    input: {
        height: 1,
    },
    searchBar: {
        padding: '5px',
        marginLeft: 'auto',
        marginRight: 0,
    },
    highlight: {
        background: 'pink',
    },
});
