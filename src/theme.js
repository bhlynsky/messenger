import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#DA0037',
    },
    secondary: {
      main: '#444444',
    },
    error: {
      main: '#171717',
    },
  },
});
export default theme;
