import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
    pageWrapper: {
        height: 'calc(100vh - 52px)',
        overflow: 'auto',
    },
    linkContainer: {
        marginTop: '50px',
    },
    container: {
        margin: 'auto',
    },
});
